
var gulp = require('gulp');

var matrix = {
	href: [ '/', '/about' ],
	lang: [ 'en-US', 'en-CA' ]
};

// Generate HTML files from all the pages
// Create gzip versions as well
gulp.task('pages', function pages() {

});
